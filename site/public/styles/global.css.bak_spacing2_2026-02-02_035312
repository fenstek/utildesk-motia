/* Utildesk — Global Styles
   Swiss Editorial Design Direction
   Pure Astro + CSS, no Tailwind, no React, no external UI libraries or fonts
*/

:root {
  /* Swiss Editorial Palette (max 4 colors) */
  --bg: #f6f3ee;
  --surface: #ffffff;
  --text: #121212;
  --accent: #0f766e;

  /* Neutral shades */
  --text-muted: #666666;
  --border: #e6e0d8;
  --border-subtle: #f0ede8;

  /* Layout */
  --radius: 12px;
  --container: 1140px;
  --container-sm: 760px;

  /* Typography - Swiss hierarchy */
  --font-sans: ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
  --font-mono: ui-monospace, "SFMono-Regular", Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
}

/* Base Reset */
*, *::before, *::after {
  box-sizing: border-box;
}

* {
  margin: 0;
  padding: 0;
}

html {
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

html, body {
  height: 100%;
}

body {
  font-family: var(--font-sans);
  color: var(--text);
  background: var(--bg);
  line-height: 1.6;
  font-size: 16px;
}

/* Typography - Swiss hierarchy */
h1, h2, h3, h4, h5, h6 {
  font-weight: 600;
  line-height: 1.2;
  letter-spacing: -0.02em;
  color: var(--text);
}

h1 { font-size: clamp(2rem, 4vw, 2.5rem); }
h2 { font-size: clamp(1.5rem, 3vw, 1.875rem); }
h3 { font-size: 1.25rem; }

p {
  line-height: 1.65;
}

a {
  color: var(--accent);
  text-decoration: none;
  transition: opacity 0.15s ease;
}

a:hover {
  opacity: 0.7;
}

/* Layout */
.container {
  max-width: var(--container);
  margin: 0 auto;
  padding: 0 24px;
}

.container-sm {
  max-width: var(--container-sm);
  margin: 0 auto;
  padding: 0 24px;
}

/* Header */
.site-header {
  position: sticky;
  top: 0;
  z-index: 100;
  background: rgba(246, 243, 238, 0.9);
  backdrop-filter: blur(8px);
  border-bottom: 1px solid var(--border);
}

.nav {
  display: flex;
  align-items: center;
  justify-content: space-between;
  height: 64px;
  gap: 24px;
}

.brand {
  font-size: 1.125rem;
  font-weight: 600;
  letter-spacing: -0.01em;
  color: var(--text);
}

.brand:hover {
  opacity: 0.7;
}

.nav-links {
  display: flex;
  gap: 24px;
  align-items: center;
}

.nav-links a {
  font-size: 0.9375rem;
  font-weight: 500;
  color: var(--text-muted);
}

.nav-links a:hover {
  color: var(--text);
  opacity: 1;
}

/* Footer */
.site-footer {
  margin-top: 120px;
  padding: 48px 0 32px;
  border-top: 1px solid var(--border);
}

.footer-content {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 16px;
  flex-wrap: wrap;
  gap: 16px;
}

.footer-brand {
  font-weight: 600;
  font-size: 1rem;
  color: var(--text);
}

.footer-links {
  display: flex;
  gap: 24px;
}

.footer-links a {
  font-size: 0.875rem;
  color: var(--text-muted);
}

.footer-copy {
  font-size: 0.875rem;
  color: var(--text-muted);
}

/* Hero Section */
.hero {
  padding: 80px 0 60px;
  text-align: center;
}

.hero-title {
  margin-bottom: 16px;
  font-weight: 600;
  letter-spacing: -0.03em;
}

.hero-subtitle {
  font-size: clamp(1.125rem, 2vw, 1.25rem);
  color: var(--text-muted);
  max-width: 640px;
  margin: 0 auto 32px;
  line-height: 1.65;
  font-weight: 400;
}

.hero-cta {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  padding: 12px 24px;
  background: var(--accent);
  color: white;
  border-radius: var(--radius);
  font-weight: 500;
  font-size: 0.9375rem;
  transition: opacity 0.2s ease;
  border: 1px solid var(--accent);
}

.hero-cta:hover {
  opacity: 0.85;
}

/* Home layout spacing */
.home-hero {
  padding: 48px 0 32px;
}

.home-hero .hero-subtitle {
  margin-bottom: 20px;
}

.home-section {
  padding: 32px 0;
}

.home-section-header {
  margin-bottom: 24px;
}

.home-tags {
  padding: 16px 0 24px;
}

.home-tags .tag-cloud {
  padding: 12px 0;
  gap: 8px;
}

@media (max-width: 640px) {
  .home-hero {
    padding: 40px 0 24px;
  }

  .home-section {
    padding: 24px 0;
  }
}

/* Cards - Swiss Editorial style (thin borders, no shadows) */
.card {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  transition: border-color 0.2s ease;
}

.card:hover {
  border-color: var(--accent);
}

/* Grid */
.grid {
  display: grid;
  gap: 24px;
}

.grid-2 {
  grid-template-columns: 1fr;
}

.grid-3 {
  grid-template-columns: 1fr;
}

@media (min-width: 768px) {
  .grid-2 {
    grid-template-columns: repeat(2, 1fr);
  }
}

@media (min-width: 1024px) {
  .grid-3 {
    grid-template-columns: repeat(3, 1fr);
  }
}

/* Section */
.section {
  padding: 60px 0;
}

.section-header {
  text-align: center;
  margin-bottom: 48px;
}

.section-title {
  margin-bottom: 12px;
  font-weight: 600;
}

.section-subtitle {
  color: var(--text-muted);
  font-size: 1.0625rem;
  max-width: 600px;
  margin: 0 auto;
  font-weight: 400;
}

/* Tag Cloud - Swiss Editorial Directory style */
.tag-cloud {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 12px;
  max-width: 800px;
  margin: 0 auto;
  padding: 32px 0;
}

.tag-cloud-item {
  display: inline-block;
  padding: 8px 16px;
  background: var(--surface);
  color: var(--text);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  font-size: 0.9375rem;
  font-weight: 500;
  transition: all 0.2s ease;
  white-space: nowrap;
}

.tag-cloud-item:hover {
  border-color: var(--accent);
  color: var(--accent);
  opacity: 1;
}

/* Tag size variants based on frequency */
.tag-cloud-item.tag-freq-high {
  font-size: 1.0625rem;
  padding: 10px 18px;
}

.tag-cloud-item.tag-freq-medium {
  font-size: 0.9375rem;
  padding: 8px 16px;
}

.tag-cloud-item.tag-freq-low {
  font-size: 0.875rem;
  padding: 7px 14px;
}

/* Tool Cards */
.tools-grid {
  display: grid;
  gap: 20px;
  grid-template-columns: 1fr;
}

@media (min-width: 640px) {
  .tools-grid {
    grid-template-columns: repeat(2, 1fr);
  }
}

@media (min-width: 1024px) {
  .tools-grid {
    grid-template-columns: repeat(3, 1fr);
  }
}

.tool-card {
  display: flex;
  flex-direction: column;
  padding: 20px;
  height: 100%;
  color: inherit;
  transition: border-color 0.2s ease;
}

.tool-card:hover {
  text-decoration: none;
}

.tool-header {
  display: flex;
  gap: 16px;
  margin-bottom: 16px;
}

.tool-image {
  width: 64px;
  height: 64px;
  border-radius: var(--radius);
  border: 1px solid var(--border);
  background: var(--surface);
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
  flex-shrink: 0;
}

.tool-image img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.tools-image-placeholder {
  width: 28px;
  height: 28px;
  border-radius: 8px;
  background: rgba(99, 102, 241, 0.3);
}

.tool-card-logo {
  width: 44px;
  height: 44px;
  border-radius: 12px;
  background: rgba(148, 163, 184, 0.12);
  display: inline-flex;
  align-items: center;
  justify-content: center;
  color: var(--text-muted);
}

.tool-card-logo img {
  width: 70%;
  height: 70%;
  object-fit: contain;
}

.tool-card-logo svg {
  width: 60%;
  height: 60%;
}

.tool-info {
  flex: 1;
  min-width: 0;
}

.tools-desc {
  font-size: 0.9rem;
  color: var(--text-muted);
  margin-top: 6px;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

.tool-title {
  font-size: 1.125rem;
  font-weight: 600;
  margin-bottom: 4px;
  color: var(--text);
}

.tool-meta {
  font-size: 0.875rem;
  color: var(--text-muted);
}

.tool-badges {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  margin-top: auto;
}

/* Home Cards */
.home-card-grid {
  display: grid;
  gap: 20px;
  grid-template-columns: 1fr;
}

@media (min-width: 640px) {
  .home-card-grid {
    grid-template-columns: repeat(2, 1fr);
  }
}

@media (min-width: 1024px) {
  .home-card-grid {
    grid-template-columns: repeat(3, 1fr);
  }
}

.home-card {
  display: block;
  padding: 18px;
  border-radius: var(--radius);
  border: 1px solid var(--border);
  background: var(--surface);
  color: inherit;
  text-decoration: none;
  transition: box-shadow 0.2s ease, transform 0.2s ease, border-color 0.2s ease;
}

.home-card:hover {
  border-color: rgba(99, 102, 241, 0.3);
  box-shadow: 0 8px 24px rgba(15, 23, 42, 0.08);
  transform: translateY(-2px);
}

.home-card-media {
  display: flex;
  gap: 16px;
  align-items: center;
}

.home-card-img {
  width: 60px;
  height: 60px;
  aspect-ratio: 1 / 1;
  border-radius: 14px;
  border: 1px solid var(--border);
  background: rgba(148, 163, 184, 0.12);
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
  flex-shrink: 0;
}

.home-card-img img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.home-card-logo {
  width: 44px;
  height: 44px;
  border-radius: 12px;
  background: rgba(148, 163, 184, 0.12);
  display: inline-flex;
  align-items: center;
  justify-content: center;
  color: var(--text-muted);
}

.home-card-logo img {
  width: 70%;
  height: 70%;
  object-fit: contain;
}

.home-card-logo svg {
  width: 60%;
  height: 60%;
}

.home-card-img-placeholder {
  width: 26px;
  height: 26px;
  border-radius: 8px;
  background: rgba(99, 102, 241, 0.3);
}

.home-card-body {
  min-width: 0;
}

.home-card-title {
  font-size: 1.0625rem;
  font-weight: 600;
  color: var(--text);
  margin-bottom: 6px;
  display: -webkit-box;
  -webkit-line-clamp: 1;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

.home-card-desc {
  font-size: 0.9rem;
  color: var(--text-muted);
  display: -webkit-box;
  -webkit-line-clamp: 1;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

/* Badges - Swiss style */
.badge {
  display: inline-flex;
  align-items: center;
  padding: 4px 10px;
  font-size: 0.8125rem;
  font-weight: 500;
  border-radius: 6px;
  white-space: nowrap;
  border: 1px solid var(--border);
  background: var(--surface);
  color: var(--text-muted);
}

.badge-category {
  border-color: var(--accent);
  color: var(--accent);
  background: var(--surface);
}

.badge-price {
  border-color: var(--border);
  color: var(--text-muted);
  background: var(--surface);
}

.badge-tag {
  border-color: var(--border);
  color: var(--text-muted);
  background: var(--surface);
}

.badge-tag.active {
  background: var(--accent);
  color: white;
  border-color: var(--accent);
}

/* Search Input */
.search-wrapper {
  margin-bottom: 24px;
}

.search-input {
  width: 100%;
  padding: 12px 16px;
  font-size: 1rem;
  font-family: var(--font-sans);
  color: var(--text);
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  transition: border-color 0.2s ease;
}

.search-input:focus {
  outline: none;
  border-color: var(--accent);
}

.search-input::placeholder {
  color: var(--text-muted);
}

/* Tag Filter Row */
.tag-filter-row {
  margin-bottom: 24px;
  padding: 16px;
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius);
}

.tag-filter-label {
  font-size: 0.875rem;
  font-weight: 500;
  color: var(--text-muted);
  margin-bottom: 12px;
  display: block;
}

.tag-filter-list {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  align-items: center;
}

.tag-filter-item {
  padding: 6px 12px;
  background: var(--surface);
  color: var(--text-muted);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  font-size: 0.875rem;
  font-weight: 500;
  cursor: pointer;
  transition: all 0.2s ease;
  text-decoration: none;
}

.tag-filter-item:hover {
  border-color: var(--accent);
  color: var(--accent);
  opacity: 1;
}

.tag-filter-item.active {
  background: var(--accent);
  color: white;
  border-color: var(--accent);
}

.clear-filter-btn {
  padding: 6px 12px;
  background: var(--surface);
  color: var(--text);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  font-size: 0.875rem;
  font-weight: 500;
  cursor: pointer;
  transition: all 0.2s ease;
}

.clear-filter-btn:hover {
  border-color: var(--text);
  opacity: 1;
}

/* Tool Detail Page */
.breadcrumb {
  margin-bottom: 24px;
  font-size: 0.9375rem;
}

.breadcrumb a {
  color: var(--text-muted);
  display: inline-flex;
  align-items: center;
  gap: 6px;
}

.breadcrumb a:hover {
  color: var(--text);
  opacity: 1;
}

.tool-detail-header {
  margin-bottom: 32px;
}

.tool-detail-title {
  margin-bottom: 16px;
}

.tool-detail-image {
  width: 100%;
  max-width: 640px;
  height: auto;
  border-radius: var(--radius);
  margin-bottom: 24px;
  border: 1px solid var(--border);
}

.tool-detail-badges {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
  margin-bottom: 24px;
}

/* Article/Content */
.article {
  padding: 32px;
  line-height: 1.7;
}


/* === Tool page: inline logo at the start of text (left, no frame) === */
.tool-float-logo{
  float: left;
  display: block;
  width: 96px;
  height: 96px;
  margin: 0 22px 12px 0;
  vertical-align: top;
  border: 0;
  background: transparent;
  box-shadow: none;
  border-radius: 0;
  overflow: visible;
  text-decoration: none;
}

.tool-float-logo img{
  width: 96px;
  height: 96px;
  object-fit: contain;
  display: block;
}

/* ensure float wraps correctly */
.article-body{ display: flow-root; }
.article h2 {
  margin-top: 32px;
  margin-bottom: 16px;
  font-weight: 600;
}

.article h3 {
  margin-top: 24px;
  margin-bottom: 12px;
  font-weight: 600;
}

.article p {
  margin-bottom: 16px;
}

.article ul,
.article ol {
  margin: 16px 0 16px 24px;
}

.article li {
  margin-bottom: 8px;
}

.article a {
  text-decoration: underline;
  text-decoration-color: var(--border);
  text-underline-offset: 2px;
}

.article a:hover {
  text-decoration-color: var(--accent);
  opacity: 1;
}

.article code {
  font-family: var(--font-mono);
  font-size: 0.9em;
  background: var(--bg);
  border: 1px solid var(--border);
  padding: 0.2em 0.4em;
  border-radius: 6px;
}

.article pre {
  padding: 20px;
  overflow: auto;
  background: var(--bg);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  margin: 20px 0;
}

.article pre code {
  background: none;
  border: none;
  padding: 0;
}

.article img {
  max-width: 100%;
  height: auto;
  border-radius: var(--radius);
  margin: 24px 0;
  border: 1px solid var(--border);
}

/* Responsive */
@media (max-width: 640px) {
  .hero {
    padding: 48px 0 40px;
  }

  .section {
    padding: 40px 0;
  }

  .site-footer {
    margin-top: 80px;
  }

  .footer-content {
    flex-direction: column;
    align-items: flex-start;
  }

  .article {
    padding: 24px;
  }

  .tag-cloud {
    padding: 24px 0;
  }
}

/* Utility Classes */
.text-center {
  text-align: center;
}

.mt-1 { margin-top: 8px; }
.mt-2 { margin-top: 16px; }
.mt-3 { margin-top: 24px; }
.mt-4 { margin-top: 32px; }

.mb-1 { margin-bottom: 8px; }
.mb-2 { margin-bottom: 16px; }
.mb-3 { margin-bottom: 24px; }
.mb-4 { margin-bottom: 32px; }

/* improve text wrapping around floated logo */
.article-body > p:first-of-type { margin-top: 0; }

/* Tool page: if the first markdown image is used as a hero/logo, let text wrap around it */
.article-body > div > p:first-child > img:first-child {
  float: left;
  width: 160px;
  max-width: 160px;
  height: auto;
  margin: 6px 18px 12px 0;
  border-radius: 14px;
  border: 1px solid var(--border);
  background: var(--surface);
}


/* === Tool page: floated favicon inside the content card === */
@media (max-width: 640px){
}
