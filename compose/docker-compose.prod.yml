services:
  motia:
    environment:
      NODE_ENV: "production"
    command:
      - bash
      - -lc
      - |
        set -e
        npm run build
        npm run start -- --host 0.0.0.0 --port 3000
